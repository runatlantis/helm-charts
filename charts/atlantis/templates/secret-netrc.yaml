{{- if .Values.netrc }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "atlantis.fullname" . }}-netrc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "atlantis.labels" . | nindent 4 }}
  {{- if or .Values.secret.annotation .Values.extraAnnotations }}
  annotations:
    {{- with .Values.secret.annotation }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.extraAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
data:
  netrc: {{ .Values.netrc | b64enc }}
{{- end }}
